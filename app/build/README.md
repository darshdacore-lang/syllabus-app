Place macOS app icons here.

- `icon.icns` is the preferred macOS icon file.
- `icon.png` may also work when building on macOS.

Run `npm run dist` from the `app/` folder to create the packaged `.dmg` and `.zip` files.
